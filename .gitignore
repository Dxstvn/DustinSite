# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# archive (local only)
archive/

# large media files (source locally, not in git)
public/videos/*.mp4
public/videos/*.webm

# root-level junk
*.mp4
!public/hero-video.mp4
*.png
!public/**/*.png

# capture-website-cli artifacts
public/images/portfolio/engine.bin
public/images/portfolio/*.html

# typescript
*.tsbuildinfo
next-env.d.ts

# About-page abstract hero backdrop — small (≈1.5–2MB), required for deploy.
# Overrides the public/videos/*.mp4|webm and *.mp4 ignores above (last match wins).
!public/videos/about-hero.mp4
!public/videos/about-hero.webm

# Personal / source photos dropped at the repo root — local only, never deploy.
/IMG_*.jpeg
/IMG_*.jpg
/Photo*.jpg
